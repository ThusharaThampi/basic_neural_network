This project implements a basic neural network from scratch using Python. The neural network is trained on small datasets with a configurable architecture, loss function, and gradient-based optimization.

